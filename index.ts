// Use the Expo Router entry which sets up the routing runtime.
// package.json `main` points to `expo-router/entry`, but importing it here
// ensures tooling and Fast Refresh work correctly.
import 'expo-router/entry';
